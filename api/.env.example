# --- Configurações de Log ---
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning

# --- JWT (Autenticação) ---
JWT__Issuer=CodeClashApp
JWT__Audience=CodeClashUsers
# Defina uma chave forte e aleatória para produção
JWT__SigningKey=sua_chave_secreta_super_segura_aqui_bem_grande_por_favor_123

# --- Judge0 (Compilador de Código) ---
# Se estiver rodando o Judge0 localmente via Docker:
Judge0__BaseUrl=http://judge0-server-1:2358/
# Se estiver usando a API pública (RapidAPI):
Judge0__ApiKey=sua_api_key_opcional
Judge0__ApiHost=judge0-ce.p.rapidapi.com

# --- Database (Supabase PostgreSQL) ---
# Exemplo de string de conexão para o Supabase (Pooler Mode - Porta 6543)
# Substitua [SEU_USUARIO], [SUA_SENHA] e [SEU_HOST] pelos seus dados reais no arquivo .env
ConnectionStrings__DefaultConnection="User Id=postgres.[seu-projeto];Password=[SUA_SENHA];Server=aws-0-sa-east-1.pooler.supabase.com;Port=6543;Database=postgres;Timeout=120;Pooling=false;"

# --- Configurações de Host ---
AllowedHosts=*